spring:
  h2:
    console:
      enabled: true

  datasource:
    url: "jdbc:h2:mem:testdb"
    username: "sa"
    password: ""

  jpa:
    hibernate:
      ddl-auto: none
      use-new-id-generator-mappings: true


  data:
    jpa:
      repositories:
        bootstrap-mode: default
