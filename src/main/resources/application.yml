spring:
#  h2:
#    console:
#      enabled: true

  datasource:
    #url: "jdbc:h2:mem:testdb"
    url: "jdbc:postgresql://localhost:5432/postgres"
    username: "postgres"
    password: "admin"


  jpa:
    hibernate:
      ddl-auto: none


  data:
    jpa:
      repositories:
        bootstrap-mode: default